`linux/quirpy.png` is a 256x256 placeholder icon, not artwork — the AppImage build simply refuses to
run without an icon. Replace it once real artwork exists.

The same artwork will later be the source for the macOS `.icns` and the Windows `.ico`; today both
bundles fall back to the OS default icon.
